import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MonitorGamerCurvoMSI49Page } from './monitor-gamer-curvo-msi-49.page';

describe('MonitorGamerCurvoMSI49Page', () => {
  let component: MonitorGamerCurvoMSI49Page;
  let fixture: ComponentFixture<MonitorGamerCurvoMSI49Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MonitorGamerCurvoMSI49Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
