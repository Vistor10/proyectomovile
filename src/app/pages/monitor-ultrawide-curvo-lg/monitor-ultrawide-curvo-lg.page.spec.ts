import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MonitorUltrawideCurvoLGPage } from './monitor-ultrawide-curvo-lg.page';

describe('MonitorUltrawideCurvoLGPage', () => {
  let component: MonitorUltrawideCurvoLGPage;
  let fixture: ComponentFixture<MonitorUltrawideCurvoLGPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MonitorUltrawideCurvoLGPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
