import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MemoriaRamKingstonFuryBeastPage } from './memoria-ram-kingston-fury-beast.page';

describe('MemoriaRamKingstonFuryBeastPage', () => {
  let component: MemoriaRamKingstonFuryBeastPage;
  let fixture: ComponentFixture<MemoriaRamKingstonFuryBeastPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MemoriaRamKingstonFuryBeastPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
