import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProcesadorIntelCoreI510400Page } from './procesador-intel-core-i5-10400.page';

describe('ProcesadorIntelCoreI510400Page', () => {
  let component: ProcesadorIntelCoreI510400Page;
  let fixture: ComponentFixture<ProcesadorIntelCoreI510400Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProcesadorIntelCoreI510400Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
