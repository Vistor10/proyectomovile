import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HyperxCloudStinger2Page } from './hyperx-cloud-stinger-2.page';

describe('HyperxCloudStinger2Page', () => {
  let component: HyperxCloudStinger2Page;
  let fixture: ComponentFixture<HyperxCloudStinger2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HyperxCloudStinger2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
