import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FuenteDePoderCLIOATX700Page } from './fuente-de-poder-clio-atx-700.page';

describe('FuenteDePoderCLIOATX700Page', () => {
  let component: FuenteDePoderCLIOATX700Page;
  let fixture: ComponentFixture<FuenteDePoderCLIOATX700Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FuenteDePoderCLIOATX700Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
