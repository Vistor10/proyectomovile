import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProcesadorIntelCoreI914900Page } from './procesador-intel-core-i9-14900.page';

describe('ProcesadorIntelCoreI914900Page', () => {
  let component: ProcesadorIntelCoreI914900Page;
  let fixture: ComponentFixture<ProcesadorIntelCoreI914900Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProcesadorIntelCoreI914900Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
