import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MemoriaRamCorsairVengeancePage } from './memoria-ram-corsair-vengeance.page';

describe('MemoriaRamCorsairVengeancePage', () => {
  let component: MemoriaRamCorsairVengeancePage;
  let fixture: ComponentFixture<MemoriaRamCorsairVengeancePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MemoriaRamCorsairVengeancePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
