import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MemoriaRamKingstonFuryRenegadePage } from './memoria-ram-kingston-fury-renegade.page';

describe('MemoriaRamKingstonFuryRenegadePage', () => {
  let component: MemoriaRamKingstonFuryRenegadePage;
  let fixture: ComponentFixture<MemoriaRamKingstonFuryRenegadePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MemoriaRamKingstonFuryRenegadePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
